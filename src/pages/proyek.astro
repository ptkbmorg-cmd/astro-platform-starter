---
import Layout from '../layouts/Layout.astro';

const projects = [
  { name: 'Instalasi CCTV', location: 'Bogor', year: '2025', scope: 'Instalasi, konfigurasi, testing' },
  { name: 'Sound System', location: 'Jakarta', year: '2025', scope: 'Instalasi, tuning, commissioning' },
  { name: 'Access Control', location: 'Depok', year: '2024', scope: 'Wiring, device setup, integration' }
];

// Upload logo klien ke: /public/clients/
// contoh nama file: /public/clients/tvri.png, /public/clients/summarecon.png, dst.
const clientLogos = [
  // { name: 'TVRI', src: '/clients/tvri.png' },
  // { name: 'Summarecon', src: '/clients/summarecon.png' },
  // { name: 'Kemenparekraf', src: '/clients/kemenparekraf.png' }
];
---

<Layout title="Proyek | PT. Khasana Berkah Mandiri">
  <section>
    <h1 style="margin: 0 0 10px; font-size: 40px; line-height: 1.15; font-weight: 900;">
      Proyek & Pengalaman
    </h1>
    <p style="margin: 0 0 22px; opacity: .92; max-width: 980px; line-height: 1.7;">
      Berikut ringkasan pengalaman pekerjaan. Silakan sesuaikan daftar proyek dan detail scope untuk portofolio resmi.
    </p>

    <div style="display:grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 14px;">
      {projects.map((p) => (
        <div
          style="
            padding: 16px;
            border-radius: 16px;
            border: 1px solid rgba(15,23,42,.10);
            background: #ffffff;
            box-shadow: 0 10px 26px rgba(15,23,42,.05);
          "
        >
          <div style="font-weight: 900; margin-bottom: 8px; line-height: 1.3;">{p.name}</div>
          <div style="opacity: .92; line-height: 1.6;">
            <div><strong>Lokasi:</strong> {p.location}</div>
            <div><strong>Tahun:</strong> {p.year}</div>
            <div><strong>Ruang lingkup:</strong> {p.scope}</div>
          </div>
        </div>
      ))}
    </div>

    <div style="margin-top: 34px;">
      <h2 style="margin: 0 0 10px; font-size: 24px; font-weight: 900;">Clients / Trusted By</h2>
      <p style="margin: 0 0 14px; opacity: .9; max-width: 980px; line-height: 1.7;">
        Tampilkan logo klien hanya jika Anda memiliki izin atau referensi publik yang memperbolehkan penggunaan logo.
      </p>

      {clientLogos.length === 0 ? (
        <div
          style="
            padding: 16px;
            border-radius: 16px;
            border: 1px dashed rgba(15,23,42,.20);
            background: rgba(255,255,255,.7);
          "
        >
          <div style="font-weight: 900; margin-bottom: 6px;">Belum ada logo klien</div>
          <div style="opacity: .9; line-height: 1.6;">
            Upload file logo ke <code>/public/clients/</code>, lalu isi array <code>clientLogos</code> di halaman ini.
          </div>
        </div>
      ) : (
        <div style="display:grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 14px; margin-top: 8px;">
          {clientLogos.map((c) => (
            <div
              title={c.name}
              style="
                height: 88px;
                display:flex;
                align-items:center;
                justify-content:center;
                padding: 10px;
                border-radius: 14px;
                border: 1px solid rgba(15,23,42,.10);
                background: #ffffff;
                box-shadow: 0 10px 26px rgba(15,23,42,.04);
              "
            >
              <img src={c.src} alt={c.name} style="max-height: 48px; max-width: 140px; object-fit: contain;" loading="lazy" />
            </div>
          ))}
        </div>
      )}
    </div>
  </section>
</Layout>
